import functions from './arrayadd.js'

test('Does that array function work?', () => {
    // expect(functions.workingWithArrays(0)).toBe(0);
    expect(functions.workingWithArrays()).toBe[99];
    // expect(workingArray.length).toBe(0); 
});